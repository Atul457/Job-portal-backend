// import { ISocket, ISocketListenerFn } from "./types.js";
/**
 * @info Holds all properties of sockets and can be used any where in the project
 */
// let socketInstance: ISocket;
/**
 *
 * @param socket Holds all socket properties
 * @info Listener for socker server
 */
const socketListener = (socket) => {
    // socketInstance = socket;
    // socket.emit("message", CONSTANTS.APP_LOG_MESSAGES.SOCKET_SERVER_REPLY);
    // socket.on("disconnect", () => {
    //   console.log("User with " + socket.id + " disconnected");
    // });
};
export { socketListener };
//# sourceMappingURL=socket.js.map